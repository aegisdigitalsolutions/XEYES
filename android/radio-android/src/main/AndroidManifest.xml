<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Declared by the module that uses the permissions, so that an app embedding this library
      inherits a matrix that matches the code in RadioPermissions rather than curating its own.
      See docs/05-android-permission-matrix.md.
    -->

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="tiramisu" />

    <!--
      BLUETOOTH_SCAN deliberately carries no neverForLocation flag. The flag would make the system
      strip location-derivable data from scan results, and BLE RSSI from fixed anchors IS the
      positioning evidence this system is built on. The cost is that location permission remains
      required on API 31+, which RadioPermissions accounts for.
    -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        tools:targetApi="s" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="s" />

    <!-- Pre-31 equivalents. maxSdkVersion keeps them out of the 31+ request set. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!--
      Every radio is optional. A phone without Wi-Fi RTT must still install and collect everything
      else: required="true" would exclude most of a realistic fleet, and the observer's capability
      declaration is what tells the Lab which sensors this device can actually see.
    -->
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    <uses-feature android:name="android.hardware.wifi.rtt" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

</manifest>
