# The app targets SDK 35 but this Robolectric release ships Android images only up to 34, and it
# refuses to infer an SDK above its own maximum. Pinning here keeps the unit tests runnable without
# lowering the app's targetSdk, which would change real runtime behaviour to suit a test harness.
sdk=34
